---
layout: nil
---

    [
    {% for post in site.posts %}
        {
          "title"    : "{{ post.title }}",
          "url"     : "{{ post.url }}",
          "date"     : "{{ post.dated }}",
          "content"  : "{{ post.content }}"
        } {% if forloop.last %}{% else %},{% endif %}
    {% endfor %}
    ] 
  